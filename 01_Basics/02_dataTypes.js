"use strict" // treat all JS code as newer version, but it runs by default

// alert(3 + 3) // we are using node.js not browser

// code readabilty should be kept in mind

// number => 2 to power 53 or 52
// bigint
// string => ""
// boolean => true/false
// null => standalone value (null error > object error)
// undefined => defined as null (value not assigned) (undefined error > undefined error)
let state = null
// symbol => unique

// object

console.log(typeof undefined);
